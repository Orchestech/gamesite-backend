const username = "";
const password = "";
const host = "";
const port = "";
const database = ""

const initOptions = {
    schema: 'public'
};

module.exports = {username, password, host, port, database, initOptions};